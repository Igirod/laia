# Configuración de la conexión a MariaDB
spring.datasource.url=${MYSQL_URL}
spring.datasource.username=${MYSQL_USER}
spring.datasource.password=${MYSQL_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# Manejo de archivos.
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=20MB

#Configuración del mail.
credential.mail.username=laiaecommercetest@gmail.com
credential.mail.password=a b g r h k e u z y m u l g h z
credential.mail.port=587
credential.mail.host=smtp.gmail.com


# Configuración de Hibernate para MariaDB
spring.graphql.graphiql.enabled=true