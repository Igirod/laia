type ProductDTO {
   id: Int!
   title: String
   price: Float
   stock: Int
   frontPage: String
   merchantId: Int
   status: Int
   createAt: String
}

type ProductInputDTO {
   id: Int!
   image: String
   price: Float
   stock: Int
   quantity: Int
}

enum TypeFilter {
   CHEAP,
   EXPENSIVE,
   NEW,
   RELEVANT
}