type ProductDTO {
   id: Int!
   title: String
   price: Float
   stock: Int
   frontPage: String
   merchantId: Int
   status: Int
   createAt: String
}

enum TypeFilter {
   CHEAP,
   EXPENSIVE,
   NEW,
   RELEVANT
}