type MerchantDTO {
   id: Int!
   email: String
   slug: String
   message: Int
   typeShipment: TypeShipment
   title: String
   image: String
   address: String
}

enum TypeShipment {
   DELIVERY,
   PICKUP,
   PICKUP_DELIVERY
}