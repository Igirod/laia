#import "./ecommerce/product.graphqls"

type InvoiceProductDTO {
   productId: Int!
   invoiceId: Int!
   quantity: Int
   product: ProductDTO
}

input InvoiceProductInputDTO {
   productId: Int!
   invoiceId: Int!
   quantity: Int
}